<?php

declare( strict_types=1 );

if ( ! function_exists( 'plugin_name_admin_settings' ) ) {
	/**
	 * Render plugin-name admin settings page.
	 *
	 * @return void
	 */
	function plugin_name_admin_settings() {}
}